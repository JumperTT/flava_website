\### flava extra 1.0帮助文档 flava拓展加载机制

\-----

flava拓展系统采用脚本与界面分离式架构，加载过程开始时，flava会先读取manifest.json的信息，再根据它加载所有.ui文件到内存，并添加到DearImgui渲染流程。flava拓展系统采用Lua5.4作为脚本解释器，它会在ui加载完成前编译所有.lua文件为.luac文件，当绑定的事件/回调触发，它会运行指定的.luac文件，整个flava进程共用一个虚拟机，共享所有全局变量，单个脚本结束不会清空全局变量，不同的拓展可以访问互相的全局变量。

